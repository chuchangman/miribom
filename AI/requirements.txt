# ── API 서버 ──────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── 데이터 / 학습 ─────────────────────────────────────────────────────────────
pandas>=1.3.0
requests>=2.25.0
Pillow>=9.0.0
tqdm>=4.60.0
matplotlib>=3.4.0
jupyter>=1.0.0
notebook>=6.4.0
ipykernel>=6.0.0
scikit-learn>=1.0.0
imagehash>=4.3.0
# torch / torchvision: CUDA 버전에 따라 별도 설치
#   GPU (CUDA 12.x): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   CPU only       : pip install torch torchvision
